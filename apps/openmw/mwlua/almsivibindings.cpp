#include "almsivibindings.hpp"

// Intentionally noncompiled in the pure bridge target. The exact implementation belongs in the
// pinned OpenMW patch after context registration and media probes are proven. It must not add a
// generic HTTP, file, process, environment, token, callback, or host-path API.
