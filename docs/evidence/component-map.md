# Component map

The machine-readable map is `component-ledger.json`. Task 1 owns source provenance, deterministic cache/bootstrap tooling, schemas, manifests, wrappers, and build entry points. It does not import an engine, implement the native bridge, or read another repository. Future implementation materializes pristine OpenMW under a generated source root and keeps ALMSIVI patches/overlays reviewable.
